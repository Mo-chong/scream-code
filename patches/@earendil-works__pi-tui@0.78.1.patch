diff --git a/dist/components/editor.js b/dist/components/editor.js
index 50ca0f8c5778e92bfde51e1724c3da0e9326d4d1..a236e10b0d7c4c367d284d6a1b8d6680ba3d6b15 100644
--- a/dist/components/editor.js
+++ b/dist/components/editor.js
@@ -489,6 +489,7 @@ export class Editor {
             }
             if (kb.matches(data, "tui.select.up") || kb.matches(data, "tui.select.down")) {
                 this.autocompleteList.handleInput(data);
+                this.tui.requestRender();
                 return;
             }
             if (kb.matches(data, "tui.input.tab")) {
diff --git a/dist/tui.js b/dist/tui.js
index b80ecfaaa512b54c599547d8b483da037570e8ca..6693fb6655de06cb67c796cbed8f3d77963b7578 100644
--- a/dist/tui.js
+++ b/dist/tui.js
@@ -121,6 +121,7 @@ export class TUI extends Container {
     overlayFocusRestore = { status: "inactive" };
     constructor(terminal, showHardwareCursor) {
         super();
+        this.fixedBottomLineCount = 0;
         this.terminal = terminal;
         if (showHardwareCursor !== undefined) {
             this.showHardwareCursor = showHardwareCursor;
@@ -892,6 +893,20 @@ export class TUI extends Container {
         };
         // Render all components to get new lines
         let newLines = this.render(width);
+        // Fixed-bottom region: pin the last N lines (editor + footer) and apply
+        // viewport scrolling only to the remaining top portion.
+        if (this.fixedBottomLineCount > 0) {
+            const bottomCount = Math.min(this.fixedBottomLineCount, height);
+            const topCount = height - bottomCount;
+            if (topCount > 0) {
+                const topLines = newLines.length > bottomCount ? newLines.slice(0, -bottomCount) : [];
+                const bottomLines = newLines.slice(-bottomCount);
+                const topStart = Math.max(0, topLines.length - topCount);
+                const visibleTop = topLines.slice(topStart, topStart + topCount);
+                while (visibleTop.length < topCount) visibleTop.push("");
+                newLines = [...visibleTop, ...bottomLines];
+            }
+        }
         // Composite overlays into the rendered lines (before differential compare)
         if (this.overlayStack.length > 0) {
             newLines = this.compositeOverlays(newLines, width, height);
@@ -905,7 +920,7 @@ export class TUI extends Container {
             let buffer = "\x1b[?2026h"; // Begin synchronized output
             if (clear) {
                 buffer += this.deleteKittyImages(this.previousKittyImageIds);
-                buffer += "\x1b[2J\x1b[H\x1b[3J"; // Clear screen, home, then clear scrollback
+                buffer += process.platform === "win32" ? "\x1b[2J\x1b[H" : "\x1b[2J\x1b[H\x1b[3J"; // Win ConPTY pins viewport to top after \x1b[3J
             }
             for (let i = 0; i < newLines.length; i++) {
                 if (i > 0)
